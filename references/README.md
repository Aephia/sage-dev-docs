# Source Material

This directory stores supplied and historical source material used while authoring the reader-facing documentation.

Files under `references/` are excluded from the VitePress build. They are evidence and research inputs, not canonical API documentation. Reader-facing claims should be checked against the generated packages and live PTR before being copied into published pages.
